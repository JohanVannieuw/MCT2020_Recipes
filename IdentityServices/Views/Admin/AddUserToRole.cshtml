@model IdentityServices.ViewModels.RolesForUser_VM

<h2>Add user to role</h2>

<form asp-controller="Admin" asp-action="AddUserToRole">
    <input type="hidden" name="roleId" value="@Model.RoleId" />
    @*<select asp-for="UserId"
                asp-items="@(new SelectList(Model.AssignedUsers, "Id", "UserName"))">
            <option>Please select a user</option>
        </select>*@
    <input class="btn" value="Add to role" type="submit" />
</form>




<a asp-action="IndexRoles" class="btn btn-primary">Return to roles overview</a>